PORT=3000
APP_ENV=development
# No App Service, configure estas variáveis nas Application settings.
DB_PROVIDER=sqlite
SQLITE_PATH=./data/loja.db
# Em produção no App Service, use DB_PROVIDER=sqlserver e configure:
# AZURE_SQL_SERVER=servidor.database.windows.net
# AZURE_SQL_DATABASE=imersao
# AZURE_SQL_USER=usuario
# AZURE_SQL_PASSWORD=senha
# Prefira identidade gerenciada e não configure usuário/senha nesse modo:
# AZURE_SQL_AUTH=managedIdentity
AI_ENABLED=false
# AZURE_OPENAI_ENDPOINT=https://seu-recurso.openai.azure.com
# AZURE_OPENAI_DEPLOYMENT=gpt-4.1-mini
